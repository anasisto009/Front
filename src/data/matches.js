// Mock match history data
// No sample match data. Real data should come from the backend API.
export const matchHistory = [];

export const upcomingMatches = [];

export const getAllMatches = () => {
  return [];
};

